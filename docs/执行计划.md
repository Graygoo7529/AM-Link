# Add/Search 执行计划

计划以“先协议正确，再提高召回质量，最后完成提交/部署”为顺序。完成项必须有验收记录，状态为 `done`。优化阶段的详细架构见 [优化方案.md](./优化方案.md)。

## P0：协议与可复现基线

| ID | 任务 | 状态 | 验收标准 | 依赖 |
| --- | --- | --- | --- | --- |
| P0-01 | 保留原有 Memory.md、daily/entity/concept/fact、links 和 inspect/recall 思想，重写完整设计文档 | `done` | [设计理念.md](../设计理念.md) 覆盖数据真源、Add、Search、时间、隔离、存储、并发、路线和提交 | 无 |
| P0-02 | 实现严格 Add/Search/Health HTTP API | `done` | FastAPI 路由、Pydantic schema、HTTP 200/401/409/422 和空结果行为通过测试 | P0-01 |
| P0-03 | 实现 SQLite/WAL 原始事件和工作记忆 | `done` | Add 事务内保存 raw event、working memory、daily node 和 FTS 索引 | P0-02 |
| P0-04 | 实现 Add 幂等与 user_id 隔离 | `done` | 相同 payload 重试不重复；payload 冲突 409；跨用户 Search 为空 | P0-03 |
| P0-05 | 实现 Memory.md/daily Markdown 可解释投影 | `done` | user scope 使用安全哈希；重复投影不重复追加；并发投影有锁 | P0-03 |
| P0-06 | 补齐 Docker、环境变量、README 和依赖 | `done` | `pip install -e ".[dev]"`、Uvicorn 启动命令和 Dockerfile 齐全 | P0-02 |
| P0-07 | 自动化协议测试与真实 HTTP smoke | `done` | 11 项 pytest 通过；本地 Uvicorn Add 后立即 Search 成功 | P0-02 至 P0-06 |
| P0-08 | 重新核对赛事官网和 Full checklist | `done` | 确认 8 月 7 日 23:59（UTC+8）、Access Request、代码/API 路径、30 天要求和 gpt-4o-mini 清单 | P0-07 |

## P1：结构化 Memory Maintenance

| ID | 任务 | 状态 | 验收标准 | 依赖 |
| --- | --- | --- | --- | --- |
| P1-01 | 增加 Add/Search 模型适配层和 no-op fallback | `done` | 默认模型固定为 `gpt-4o-mini`；模型失败仍返回合法 raw/lexical 证据；配置不允许替换模型 | P0-08 |
| P1-02 | 接入固定 `gpt-4o-mini` 的 Add structured mutation | `done` | schema 只接受 fact/source/entity/concept/version/tombstone proposal；每条 fact 有 source event；Add 同步等待 | P1-01 |
| P1-03A | 接入固定 `gpt-4o-mini` 的 Search query/rerank | `done` | Search 模型只产生 query plan 与 intent；最终输出均来自已存证据 | P1-01 |
| P1-04 | 在事务中应用 entity/concept/fact 和 typed links | `done` | 稳定 ID；跨 user supersede 被拒绝且整批回滚；typed links 有测试 | P1-02 |
| P1-05 | 实现 fact version、valid time、confidence、status | `done` | current/historical、superseded、tombstoned 及索引删除有回归测试 | P1-04 |
| P1-06 | 投影 entity/concept/fact Markdown | `done` | 结构节点投影含状态、版本、有效期和 source event；数据库仍是真源 | P1-04 |

## P2：混合召回与多步骤 Search

| ID | 任务 | 状态 | 验收标准 | 依赖 |
| --- | --- | --- | --- | --- |
| P2-01 | 增加 embedding provider 接口 | `done` | provider 可关闭；无 embedding key 时 lexical 正常运行；不改变官方 API | P0-08 |
| P2-02 | 建立 raw/fact/entity/concept 向量索引 | `done` | 增量索引、tombstone 删除和事务式全量/单用户离线重建均有测试或命令 | P2-01、P1-04 |
| P2-03 | 实现 lexical + semantic RRF/rerank | `done` | FTS、embedding 和 graph 使用加权 RRF；结果不超过 top_k | P2-02 |
| P2-04 | 实现受限 inspect/recall | `done` | planner 前一跳 inspect；最终前 20 个种子，普通最多两跳、multi-hop 最多三跳；只返回 source evidence | P1-03A、P2-03 |
| P2-05 | 增加时间解析和冲突排序 | `done` | 相对时间保留原文；current 与 historical intent 控制 superseded 召回 | P1-04、P2-03 |

## P3：评测与工程化

| ID | 任务 | 状态 | 验收标准 | 依赖 |
| --- | --- | --- | --- | --- |
| P3-01 | 导入赛事公开数据并构造 Add/Search 回放 | `in_progress` | 赛事 Smoke 数据仍由平台固定提供且未公开下载；LoCoMo 3-sample 扩大基线已完成（101 Add / 138 Search），等待主办方 Smoke 入口做最终对照 | P2-05 |
| P3-02 | 压测 64 Add / 32 Search 并发 | `todo` | 记录吞吐、P95、锁等待、429/5xx 和内存占用 | P0-07、P2-05 |
| P3-03 | 完成重试、deadline、日志脱敏和故障降级 | `in_progress` | 提供方重试/超时/脱敏/降级、failed maintenance 显式 retry、enrichment job、请求级 Add/Search deadline、provider 调用计数和 30 天清理已完成；仍需导出延迟/失败率运行指标 | P1-02、P2-02 |
| P3-04 | 固定依赖、prompt、配置和 commit | `todo` | Docker 从干净环境构建；不含任何 Key 或评测数据 | P3-03 |
| P3-05 | 实施下一阶段 Search/ADD 优化 | `done` | 已实现检索约束 maintenance、inspect-first planning、扩展 embedding、preferred link seed、受控三跳 GraphPath 与 source-event 折叠；52 项测试和真实 v7 增强 smoke 通过；3-sample lexical Recall@10 维持 0.508696 | P3-01 |

## P2-O：Evidence-first 优化

| ID | 任务 | 状态 | 验收标准 | 依赖 |
| --- | --- | --- | --- | --- |
| P2-O1 | 增加 canonical key 和 evidence group 字段 | `done` | 旧库自动迁移；新旧节点可读；同一事实的派生节点可归并 | P1-04 |
| P2-O2 | Search 结果按 evidence group 去重并保留直接证据 | `done` | 重复事实不占满 top-k；多跳链仍能保留必要节点 | P2-O1 |
| P2-O3 | 改善中文、数字、日期和否定词 token 化 | `done` | 中文实体和短语回归测试通过；英文行为不回归 | P2-03 |
| P2-O4 | 增加确定性时间表达和查询状态过滤 | `done` | 明确日期、今天/明天/昨天、上下周星期、上下月可解析；使用最新 source timestamp 锚定；temporal 候选进入 Search 融合 | P2-05 |
| P2-O5 | 增加持久化 enrichment job 和请求级 deadline | `done` | `sync` 默认保持原行为；`async` 模式 raw/FTS 先返回并由持久化 worker 消费；Add/Search 可选阶段受总 deadline 约束；失败 job 可重试 | P3-03 |
| P2-O6 | 公开数据回放与去重/时间/多跳指标 | `done` | LoCoMo 10 对话、480 题 HTTP 回放完成；recall/MRR/重复率/时间/多跳/分类型指标已记录于 [LoCoMo 回放](./LoCoMo回放.md)；赛事私有/Smoke 数据仍待官方入口 | P2-O2、P2-O4 |

## P3-L：LoCoMo 自评基线

| ID | 任务 | 状态 | 验收标准 | 依赖 |
| --- | --- | --- | --- | --- |
| P3-L1 | 接入官方 LoCoMo 数据转换器 | `done` | 原始 JSON 转为 20-message Add chunk；按 `dia_id` 生成证据 target；脏 evidence 可规范化并有测试 | P2-O5 |
| P3-L2 | 完成 lexical HTTP 基线 | `done` | 10 对话、399 Add、480 Search 全部 HTTP 200；延迟和 recall 指标落盘 | P3-L1 |
| P3-L3 | 真实 embedding-3 对照 | `done` | 允许网络的隔离服务完成 5 Add / 6 Search；5 个 job completed、76 个向量写入；结果见 [LoCoMo 回放](./LoCoMo回放.md) | P3-L1 |
| P3-L4 | 调试模型完整 Add/Search 对照 | `done` | `gpt-5.4-mini`（非官方调试模型）+ `embedding-3` 完成 5 Add / 6 Search；结构节点、向量和逐题 rank 已记录 | P3-L3 |
| P3-L5 | 正式模型提供方接入与 Full 对照 | `done` | 响应模型确认为 `gpt-4o-mini`；完整 Add/Search 生成结构节点、links 和 512 维向量；固定切片 5 Add / 6 Search 全部 200，Recall@10 0.667 | P3-L4 |

## P4：参赛交付

| ID | 任务 | 状态 | 验收标准 | 依赖 |
| --- | --- | --- | --- | --- |
| P4-01 | 代码提交包 | `todo` | 公开仓库、固定 commit、README、Docker 启动和 Smoke 通过 | P3-04 |
| P4-02 | 自部署 API（备选） | `todo` | HTTPS、公网 Add/Search、Memory System Key、持久盘和 30 天监控 | P3-04 |
| P4-03 | 提交前官方页面人工复核 | `done` | 官网确认截止为 2026-08-07 23:59（UTC+8）、Access Request、代码/API 路径和提交清单 | 官网可访问；公众号正文仍待人工比对 |

## 下一执行顺序

```text
canonical/evidence 归并 -> 中文 token 与时间语义 -> enrichment job/deadline
-> 公开数据回放 -> P3-05 Search/ADD 优化 -> maintenance 稳定性与并发压测
-> P3-04 固定版本 -> P4 提交/部署
```

P1、P2 和正式提供方接入均已完成；P3 正在进行 maintenance 稳定性、links 消融和容量校准。P4 仍依赖公开评测入口和部署基础设施。P4-03 已完成官网核对；公众号正文若包含补充规则，提交前仍需人工比对。
