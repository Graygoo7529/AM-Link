# Agent Memory Add/Search 设计理念与技术路线

本方案只负责记忆写入与记忆检索，即比赛协议中的 Add/Search；最终回答 Answer、评分 Eval 与评测编排由主办方完成。

系统不试图在 Search 中回答问题，而是建立一个长期运行、可追溯、可治理的记忆空间，并向统一 Answer 模型返回最有价值的记忆证据。

整体设计仍然围绕快速记忆、结构化记忆和多步骤搜索展开；为了适应统一评测，进一步引入原始事件、用户隔离、幂等事务、版本化事实和证据输出层。

## 设计原则

原始消息是事实真源；Memory.md、daily、entity、concept、fact 都是可以由原始消息重新构建的派生记忆；

记忆始终属于一个明确的 user_id；任何文档、索引、链接和缓存都不能跨 user_id 共享；

Add 成功意味着消息已经持久化并且能够被 Search 立即找到；不能依赖返回 HTTP 200 后尚未完成的后台任务；

Search 只选择、展开和排序记忆证据，不针对当前问题生成最终答案；

Full 合规版本在 Add 与 Search 内部使用的模型固定为 `gpt-4o-mini`；纯 lexical/no-model 实现只作为协议 Smoke 和故障降级基线，不能直接作为 Full 参评版本；

结构化记忆不能以丢失历史为代价；最新状态和历史状态都要保留，并通过有效期和状态区分；

所有由 Agent 产生的结构化内容都必须能够追溯到原始事件；没有证据来源的内容不能进入正式 Search 结果；

系统应在模型不可用、maintenance 超时或结构化结果不完整时，退化到原始消息检索，而不是整体失败。

## 原始事件（raw events）

每一次 Add 都会收到 request_id、messages、user_id 和 session_id；messages 按原始顺序保存，每条消息保留 role、content 和可选 timestamp；

原始事件不可被 Memory maintenance 改写；结构化记忆可以更新、合并和失效，但必须保留 source event links；

每条原始事件具有稳定 ID，ID 由 user_id、request_id 和消息序号稳定生成；重复 Add 不会创建重复事件；

原始事件同时建立基础全文或语义索引，保证即使结构化 maintenance 尚未产生某条事实，Search 仍然可以从原文中找回证据；

role 是证据的一部分；用户陈述、助手陈述和系统性内容不能在结构化时被无差别视为用户事实；

session_id 用于保存来源会话边界，但 Search 的唯一隔离边界仍然是 user_id。

## 快速记忆（ADD）

每个 user_id 维护一份显式工作记忆 Memory.md，跨 Add 连续；Memory.md 是近期上下文和 memory links 的可解释视图，而不是唯一数据源；

ADD 动作首先原样保存本次 messages，再增量更新 Memory.md；更新应尽量基于已有 memory links，避免反复创建语义重复的结构化节点；

Memory.md 记录最近事件、尚未完成结构化的线索以及当前活跃的 entity/concept/fact links；达到事件数、字符数或 token 阈值后触发 Memory maintenance；

触发条件以 user_id 内的累计规模为准，不依赖评测服务器当天日期；比赛数据可能包含历史日期，也可能没有 timestamp；

Memory maintenance 完成后可以重置近期缓冲区，但不能删除原始事件和已建立的基础索引；重置后的 Memory.md 应保留当前活跃 links 和 maintenance watermark；

同一个 user_id 的 Add 使用串行事务或 per-user 锁，避免多个分块同时编辑工作记忆；不同 user_id 可以并行处理；

Add 通过 request_id 保证幂等；相同 request_id 和相同请求体重复到达时返回原成功结果，相同 request_id 携带不同请求体时返回冲突错误；

Add 只有在原始消息、工作记忆和最低可用检索索引原子提交后才返回 HTTP 200。

## 结构化记忆（memory links）

所有结构化记忆在上下文中通过 link 呈现，例如 memory:entity@<stable-id>、memory:daily@YYYY-MM-DD、memory:fact@<stable-id>；

link 的标识使用稳定且路径安全的 ID；名称或 fact summary 只作为属性，不能作为可变主键；

所有 link 都属于 user_id 命名空间；相同名称的实体在不同 user_id 中是完全不同的节点；

所有 link 都映射到一个逻辑 Markdown 文档；Markdown 文档可由数据库重新投影，用于人工检查、复现和 Agent inspect；

link 属性对应 Markdown YAML/front matter，也对应数据库中的结构化字段，例如 created_at、updated_at、event_time、confidence、activity、status 和 source_event_ids；

memory links 之间允许建立 typed links，例如 mentions、about、supports、supersedes、related_to 和 occurred_on；反向链接由索引派生，不在多个文档中重复维护真源；

### memory:daily@YYYY-MM-DD

daily 是按事件时间组织的持久化日志；保存接近原文的事件证据和 source event links，原则上只追加；

日期优先来自消息 timestamp，其次来自文本中明确且可信的日期；无法确定时进入 memory:daily@undated，不能使用 ingested_at 冒充事件日期；

上午、下午、晚上只在 timestamp 和时区足够明确时作为浏览视图；底层始终保存精确 timestamp 和原始时间表达；

较晚到达的历史消息仍然可以追加到过去日期的 daily 中，因此“只追加”不等于“只写今天”；

daily 可以保留审计证据，但被遗忘、删除或隐私规则覆盖的事件必须在 Search 层被 tombstone 阻断。

### memory:entity@<stable-id>

entity 表示人、物、地点或组织；包含规范名称、别名、类型、首次和最近出现时间以及相关 memory links；

实体合并必须保守；同名不等于同一实体，缺少证据时保留多个候选节点；

实体正文不直接堆积全部历史，而是维护摘要、活跃事实和指向 daily/fact 的证据链接。

### memory:concept@<stable-id>

concept 表示主题、抽象概念、领域或长期兴趣；用于连接不共享同一实体但语义相关的事件；

concept 可以支持语义检索和多跳扩展，但不能仅凭主题相似度把不同 user_id 或不同事实状态混合。

### memory:fact@<stable-id>

fact 表示可独立引用的原子事实；正文至少包含 source event 或 daily link，并尽量包含 entity/concept links；

fact 不直接覆盖旧值，而是通过 version、valid_from、valid_to 和 status 表达变化；

status 至少包含 active、superseded 和 tombstoned；当前问题可以优先 active，历史问题仍可召回 superseded，tombstoned 默认永不返回；

事实中的事件时间、来源时间、写入时间和维护时间分别保存，不能混成一个 updated_at；

confidence 表示结构化解析的可信度，不能替代检索相关性；activity 表示近期使用或更新强度，不能抹去低活跃但与问题直接相关的事实。

## Memory maintenance

Memory maintenance 是对近期原始事件和已有 memory links 的增量整理，而不是对全部历史的反复总结；

Full 版本的 Add maintenance 使用固定的 `gpt-4o-mini` 生成受约束 mutation；模型调用失败时仍保留原始事件和 lexical 索引，保证同步协议与降级路径；

maintenance 输入包括新增原始事件、Memory.md、先由 lexical/embedding 找到的相关结构节点、最近节点补集和一跳 links；已有节点同时公开稳定 ID、canonical key、版本、置信度和来源，模型只能 supersede/tombstone 本轮实际召回的节点，不会默认把整个用户历史放入模型上下文；

当前实现不引入 pi SDK：Add 使用一次结构化 maintenance 调用，Search 使用一次 query-plan 调用，link inspect/recall 由确定性代码执行；只有公开回放证明多步骤 Agent 的收益超过延迟、成本和不确定性时才引入，并继续固定 gpt-4o-mini、最大步骤数和总 deadline；

Agent 的输出不是自由编辑文件，而是一组可校验的结构化 mutation：新增节点、更新节点、增加链接、标记 superseded、标记 tombstoned；

每个 mutation 必须声明 source_event_ids；数据库在事务中校验 user_id、节点状态和引用完整性后再应用；

maintenance 应尽量复用已有 links；只有当候选节点与现有节点无法可靠合并时才新增；

maintenance 设置模型调用数、最大步骤数、token 和墙钟时间预算；预算耗尽时提交已经验证的 mutation，剩余内容继续保留在工作记忆中；

如果模型失败，Add 仍可依靠原始事件和基础索引满足立即可检索要求；失败状态写入 maintenance 日志，后续可以重试，但不能重复创建节点；

遗忘、删除、冲突修正和隐私指令属于 maintenance 的一等任务；它们通过版本状态和检索策略生效，而不是直接破坏审计真源。

## 从结构化记忆里搜索（SEARCH）

SEARCH 由候选召回、memory link inspect、memory recall、图扩展、重排和证据输出组成，也可以引入受限的轻量 Agent；

Full 版本的 Search 模型调用（查询理解、候选选择或重排）同样固定为 `gpt-4o-mini`；它只能产生候选 ID、排序或检索参数，不能生成最终答案；

Search 请求中的 query 是检索问题；options 只作为可选检索辅助信息，不能被改写成最终答案；所有检索只能发生在完全相同的 user_id 下；

### 候选召回

第一路从原始事件和 daily 中进行全文/BM25检索，保证具体姓名、数字、否定词和未结构化信息可被直接找回；

第二路通过 embedding 检索 raw events、fact、entity 和 concept，解决同义表达和长距离相关性；

第三路可以按时间、session 和活跃状态补充候选，但不能用 ingested_at 替代问题真正涉及的事件时间；

### memory link inspect action

inspect 从候选 memory links 出发，在 user_id 内逐步探索 links；支持 entity/concept 反向链接、fact 证据链接和 daily 时间邻接；

Search 在 query plan 前先从直接候选执行一次一跳 inspect，使正向链接和 backlinks 能进入模型的有界候选集；最终图扩展默认最多两跳，明确 multi-hop 查询最多三跳，并限制每个节点的最大分支数；只有与 query 保持相关的节点才能进入下一跳；

inspect Agent 只产生候选 ID 和选择理由，不产生对用户问题的最终回答。

### memory recall action

recall 按稳定 ID 精确读取 daily、fact、entity、concept 或原始事件；读取时执行 user_id 和 status 校验；

fact recall 可以同时返回必要的 source event 片段；entity/concept recall 应解析为人类可读证据，不能只把 memory:... link 字符串交给 Answer 模型；

tombstoned 节点和被隐私规则阻断的原始事件不会进入 recall 结果。

### 重排与证据输出

候选按照词法相关性、语义相关性、图距离、时间有效性、置信度和重复度重排；

优先返回直接证据，并为多跳问题保留构成完整关系链所需的多条证据；不能为了高 precision 只返回链条中的最后一条；

返回数量不超过请求 top_k，正式评测 top_k 为 100；top_k 是上限而不是必须填满的目标；

每项结果具有稳定 id、非空 content，可选 score 和 created_at；data 按最终相关性降序排列；

content 应包含足够的角色、来源和时间信息，并尽量保留原文措辞；不返回内部 chain of thought、选择理由或针对 query 生成的答案；

当 Agent、embedding 或图检索超时，Search 退化到基础全文检索和最近直接证据，并仍然返回合法 JSON。

## 时间语义

event_time 表示事件发生时间；source_time 表示消息 timestamp；ingested_at 表示 Add 接收时间；created_at/updated_at 表示结构化节点生命周期；

原文中的 yesterday、last month 等相对表达必须保留；可以额外记录解析后的候选绝对时间，但不能覆盖原始表达；

冲突事实按照 valid_from/valid_to 和 source evidence 组织；Search 根据问题是当前态还是历史态决定召回 active 或 superseded 版本；

无时间信息的事实不能被赋予虚构事件时间；其排序可以使用 session 顺序，但必须标记时间未知。

## 数据隔离与生命周期

user_id 是唯一检索范围；数据库查询、全文索引、向量索引、图邻接和 Markdown 路径都必须显式携带 user_id；

user_id 作为不透明字符串处理；用于路径时只使用安全哈希，不解析其中可能存在的 run、dataset 或 conversation 语义；

每个正式 Run 的 user_id 自然形成独立数据空间；不同 Run 不复用评测记忆；

接口日志不能记录 Memory System Key 或 OpenAI Key；正文日志默认只记录长度、哈希、耗时和 ID；

评测数据只用于当前任务；任务完成后按照主办方要求在 30 天内删除原始事件、派生记忆、索引和备份。

## 存储与可解释视图

第一阶段使用 SQLite/WAL 作为事务真源，适合单容器、平台部署和中等规模评测；每次请求使用独立连接并设置 busy timeout；

核心表包括 add_requests、raw_events、working_memory、memory_nodes、memory_links、maintenance_runs 和全文索引；

数据库使用 user_id 与 request_id 唯一约束保证隔离和幂等；所有写入在事务中完成；

Markdown 不参与事务真源；提交成功后由 projector 从数据库生成 Memory.md、daily、entity、concept 和 fact 文档；投影失败只记录错误，可在之后完整重建；

当 SQLite 写入、索引规模或多实例部署成为瓶颈时，可将事务真源迁移到 PostgreSQL、向量索引迁移到 Qdrant，而不改变 Add/Search 协议和 memory link 模型。

## 并发与一致性

平台可能同时发起大量 Add/Search；不同 user_id 应并行，同一 user_id 的写入必须按事务顺序提交；

Search 在读取时只观察已经成功提交的 Add；maintenance 更新通过新版本原子替换活跃视图，避免读取半完成结构；

request_id 重试不能重复追加 daily、重复计数 activity 或再次收取不必要的模型费用；

服务必须限制内部模型并发，处理 gpt-4o-mini 的 RPM/TPM 和 429；外层 HTTP 并发不能无界传递到模型提供方；

正式部署需要共享持久存储；如果使用会横向扩容的无状态容器，不能依赖实例本地 Markdown 或 SQLite 文件。

## Add 接口协议

Add 使用 POST JSON：

```json
{
  "request_id": "eval:run_abc123:dataset:conv-0:chunk-0",
  "messages": [
    {
      "role": "user",
      "timestamp": 1704067200000,
      "content": "memory text"
    }
  ],
  "user_id": "eval:run_abc123:dataset:conv-0",
  "session_id": "eval:run_abc123:sample:0"
}
```

messages 非空，role 和 content 必填，timestamp 为可选 Unix 毫秒；现行协议不依赖 metadata、app_id、agent_id 或 async_mode；

成功时只有在全部消息可立即检索后返回：

```json
{
  "success": true,
  "request_id": "eval:run_abc123:dataset:conv-0:chunk-0",
  "user_id": "eval:run_abc123:dataset:conv-0",
  "session_id": "eval:run_abc123:sample:0"
}
```

成功状态必须为 HTTP 200；三个 ID 原样返回；不返回 HTTP 202、task ID、轮询地址或 memory_ids。

## Search 接口协议

Search 使用 POST JSON：

```json
{
  "query": "Which answer best matches the memory?",
  "options": ["A. First answer", "B. Second answer"],
  "user_id": "eval:run_abc123:dataset:conv-0",
  "top_k": 100
}
```

query、user_id 和 top_k 必填；options 仅在选择题中出现；现行协议不依赖 filters、rerank 或 keyword_search；

成功响应为：

```json
{
  "data": [
    {
      "id": "mem_01H...",
      "content": "[2024-01-01][user] remembered fact text",
      "score": 0.87,
      "created_at": "2024-01-01T12:00:00Z"
    }
  ]
}
```

data 必须是数组，没有结果时返回空数组；id/content 必填；结果数量不超过 top_k；score 越大表示越相关；

Search 不生成最终答案，不使用金标，不跨 user_id 检索，不返回 tombstoned 或被隐私规则阻断的内容。

## 鉴权与健康检查

Add/Search 支持 Authorization: Token、Authorization: Bearer 或 X-Api-Key；正式部署使用独立的 Memory System Key；

Health 使用无需鉴权的 GET，任意 2xx 表示健康；默认提供 /health；

服务地址使用公网 HTTP(S)，生产环境使用 HTTPS；URL 不包含用户名、密码或密钥。

## 错误、重试与降级

格式错误返回 400/422，鉴权失败返回 401，跨范围或权限拒绝返回 403，幂等冲突返回 409；

Add 对临时数据库锁、模型 408/425/429/5xx 和 transport failure 进行有限退避，但总耗时受内部绝对 deadline 限制；容量 semaphore 等待、HTTP、embedding batches 和退避都消耗同一预算；

Search 的 embedding 或 planner 失败时保留已获得候选并退化到词法/时间检索，不因可选能力失败返回 5xx；SQLite 暂时不可用则返回可重试的 503；

错误响应不包含密钥、完整提示词或大段评测正文；日志保留 request_id、user_id 哈希、阶段、耗时和错误类型；

接口的主办方客户端超时为 1200 秒，内部目标远低于该上限；Full 总任务需要在 72 小时内完成。

## 技术架构

API 层负责 schema、鉴权、状态码和协议响应；

Application 层负责 Add/Search 用例、deadline、降级和指标；

Repository 层负责 SQLite 事务、幂等、隔离、版本状态和全文检索；

Maintenance 层负责 working memory 和结构化 mutation，第一版允许关闭 LLM 并保留原文检索基线；

Retrieval 层负责 lexical、embedding、graph inspect、recall、rerank 和去重；

Projection 层负责把数据库状态生成为 Markdown memory links 视图；

所有可选能力通过接口注入；未配置模型或向量服务时，基础 Add/Search 仍可以独立运行和通过协议 Smoke。

## 当前落地架构

Add 的同步顺序是 per-user 哈希锁、raw/working/FTS 原子提交、gpt-4o-mini maintenance、结构 mutation 事务、embedding-3 分块索引、Markdown 投影和 HTTP 200；同 request_id 重放只返回已保存响应，不再次调用模型或 embedding。`AML_ENRICHMENT_MODE=async` 时，HTTP 200 只等待 raw/working/FTS 硬提交，模型/embedding 由持久化 enrichment job 和受控 worker 继续处理；两种模式都保留 failed 状态和 deadline 降级。

maintenance 输入只包含本次原始事件、受 16000 字符限制的 Memory.md 和 lexical/embedding/最近补集找到的结构节点及一跳 links；候选附带 canonical key、版本、置信度、活跃度和来源，输出由 Pydantic 严格校验为 fact proposal、source ordinal、entity/concept、valid time、supersedes 和 tombstone，Maintenance 先验证 mutation 只引用本轮召回 ID，Repository 再验证所有引用属于同一 user_id；

Search 的同步顺序是 FTS/embedding 初召回、按最新 source timestamp 锚定的明确/相对时间候选召回、从直接候选做一次有界一跳 link inspect、把直接与 link 候选摘要交给一次 gpt-4o-mini query plan、用 planner 扩展词重跑 lexical/embedding 和状态过滤、优先用 preferred memory IDs 构造前 20 个图种子、按 intent 执行普通最多两跳或 multi-hop 最多三跳的受限扩展、特征/RRF 融合、evidence group 去重和 top_k 截断；模型只输出检索扩展和候选 ID，不生成答案。memory_nodes 额外保存 canonical_key、evidence_group_id、time_expression 和 resolved time range，用于同一事实的稳定合并、证据结果去重和时间检索；中文查询保留连续词并补充二元/三元片段；planner 或扩展 embedding 不可用时由已有初召回和确定性 intent 降级完成合法 Search。

OpenAI 兼容适配器调用 `${OPENAI_BASE_URL}/chat/completions`，模型名在配置校验和请求体中双重固定为 gpt-4o-mini；智谱适配器调用 `${ZHIPU_BASE_URL}/embeddings`，模型固定为 embedding-3，默认 512 维、最多 64 条一批；

两类提供方均有进程内 semaphore、有限指数退避、HTTP timeout 和请求级绝对 deadline；排队、请求、批处理和退避不会各自重新获得完整预算。提供方响应正文、用户消息和密钥不进入错误日志。模型、embedding、投影任一可选阶段失败时，已经提交的 raw evidence 仍可通过 lexical Search 返回。持久化 enrichment job 默认最多尝试 5 次；sync 模式恢复 failed，async 模式恢复 pending/failed，并以最新 watermark 重建用户派生索引，防止旧任务覆盖新状态。

所有候选来源在业务得分相同后使用 created time、event sequence 和稳定 memory ID 做确定性裁决。结构化 fact 只作为检索桥梁和可解释摘要；高置信 fact 入选时，其已保存的直接 source event 获得有界伴随提升，使 Search 同时返回可追溯原文，但不扩大 user scope，也不生成新证据。

## 第一阶段实现路线

实现 FastAPI Add/Search/Health 和 Token、Bearer、X-Api-Key 鉴权；

实现 SQLite/WAL、原始事件、工作记忆、幂等请求、user_id 隔离和稳定 memory ID；

把每条原始事件作为 daily evidence 建立全文索引，并生成 Memory.md/daily Markdown 投影视图；

实现 lexical Search、稳定排序、top_k、证据 JSON 和零结果响应；

实现契约、幂等、隔离、时间戳、鉴权和检索测试；

提供 Dockerfile、环境变量示例、启动命令和 Smoke 请求示例。

## 第二阶段实现路线

接入 embedding 索引，形成 raw/fact/entity/concept 多路召回；

接入 Add 与 Search 均固定为 gpt-4o-mini 的模型适配层，其中 Add 生成 maintenance mutation，Search 只生成查询理解/候选排序结果；

实现 fact 版本、superseded/tombstoned、实体别名、concept 和 typed links；

实现受限的一至两跳 inspect、精确 recall 和可解释重排；

使用公开子集校准 maintenance 阈值、召回数量、图扩展宽度、deadline 和模型并发。

## 提交与复现

学术代码提交需要公开仓库、固定 commit、README、Docker 构建和启动命令、API 入口、依赖、配置、方法来源和改动说明；

学术自行部署还需要稳定公网 Add/Search 地址、鉴权方式和至少 30 天可访问性；

正式 Full 前必须通过 Smoke，固定模型、prompt、依赖和配置；Full 受理后不再因结果变化替换版本；

任何复现环境都不把 OpenAI Key、Memory System Key 或评测数据写入仓库和镜像。
